<div class="container list">
  <div class="d-flex">
    <%= link_to root_path, class: 'back-link' do %>
      <i class="fa-solid fa-arrow-left me-2"></i>
    <% end %>
    <h1>Instrumentos Disponíveis</h1>
  </div>

  <div class="table-grid-wrapper">
    <% @instruments.each_with_index do |instrument, index| %>
      <div class="table-grid-item first"><%= index + 1 %></div>
      <%= link_to instrument_path(instrument) do%>
        <div class="table-grid-item second"><%= instrument.title %></div>
      <% end %>
    <% end %>
  </div>
</div>
